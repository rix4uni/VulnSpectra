# code-review

# Usage
```console
Usage of code-review:
  -ext string
        Comma-separated list of file extensions to include (e.g., php,java)
  -filesystem string
        Directory to scan (default ".")
```
